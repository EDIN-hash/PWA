# 📚 Документация Inventory PWA

## 📁 Структура проекта

```
inventory-pwa/
├── public/                  # Статические файлы
├── server/                  # Backend (Express.js)
│   ├── DataBase.db3         # База данных SQLite
│   ├── db.js                # Инициализация БД
│   └── server.js            # Основной сервер
├── src/                     # Frontend (React)
│   ├── components/          # UI компоненты
│   ├── App.jsx              # Главный компонент
│   ├── Card.jsx             # Карточка товара
│   ├── styles.css           # Основные стили
│   └── ...                  # Другие файлы
├── service-worker.js        # PWA service worker
├── manifest.webmanifest     # PWA манифест
├── vite.config.js           # Конфигурация Vite
├── package.json             # Зависимости
└── index.html               # Основной HTML
```

## 🚀 Запуск проекта

### Установка зависимостей
```bash
npm install
```

### Запуск frontend
```bash
npm run dev
```

**Примечание:** Vite автоматически выберет свободный порт, начиная с 5173. Если порт занят, он попробует порты 5174-5183.

### Запуск backend
```bash
cd server && node server.js
```

**Примечание:** Сервер автоматически выберет свободный порт, начиная с 3001. Если порт занят, он попробует порты 3002-3006.

### Запуск всего проекта
```bash
npm start
```

Эта команда запустит и frontend, и backend одновременно.

## 🎯 Основные возможности

### 1. Frontend (React + Vite)

**Главный компонент**: `App.jsx` - управляет всей логикой приложения

**Основные функции:**
- Аутентификация пользователей (login/register)
- Управление товарами (CRUD операции)
- Фильтрация по категориям
- Поиск товаров
- Темная тема
- Адаптивный дизайн

**Компоненты:**
- `Card.jsx` - Карточка товара с полной информацией
- `Modal` - Модальные окна для добавления/редактирования
- `DatePicker` - Выбор даты

### 2. Backend (Express.js + SQLite)

**API маршруты:**
- `GET /items` - Получение товаров
- `POST /items` - Добавление товара
- `PUT /items/:name` - Обновление товара
- `DELETE /items/:name` - Удаление товара
- `POST /users/register` - Регистрация
- `POST /login` - Аутентификация
- `POST /users/logout` - Выход

**База данных:**
- Таблица `users` - Пользователи
- Таблица `items` - Товары

### 3. Аутентификация

**Роли пользователей:**
- `admin` - Полный доступ
- `spectator` - Только просмотр

**Безопасность:**
- Хэширование паролей (bcrypt)
- Сессионная аутентификация

### 4. PWA возможности

- Service Worker для оффлайн работы
- Web App Manifest для установки
- Кэширование ресурсов

## 📱 Мобильная адаптация

Проект полностью адаптирован для мобильных устройств:
- Отзывчивый дизайн
- Тач-оптимизация
- Адаптивная сетка
- Оптимизация для разных ориентаций

## 🎨 UI/UX улучшения

### Темная тема
- Переключатель в шапке
- Сохранение предпочтений в localStorage
- Полная поддержка всех компонентов

### Анимации
- Плавные переходы
- Hover эффекты
- Анимации загрузки
- Ripple эффект для кнопок

### Дизайн
- Современные карточки товаров
- Визуальные индикаторы статусов
- Иконки и бейджи
- Лучшая типографика

## 🔧 Технический стек

**Frontend:**
- React 18
- Vite
- Tailwind CSS
- DaisyUI

**Backend:**
- Express.js
- SQLite
- bcrypt
- JWT (сессионная аутентификация)

**PWA:**
- Service Worker
- Web App Manifest
- Offline support

## 📝 Документация по файлам

### src/App.jsx

**Основной компонент приложения**

**Состояния:**
- `currentUser` - Текущий пользователь
- `items` - Список товаров
- `darkMode` - Темная тема
- `selectedCategory` - Выбранная категория

**Функции:**
- `fetchItems()` - Загрузка товаров
- `handleLogin()` - Аутентификация
- `handleRegister()` - Регистрация
- `toggleDarkMode()` - Переключение темы

### src/Card.jsx

**Компонент карточки товара**

**Пропсы:**
- `item` - Данные товара
- `editItem` - Функция редактирования
- `deleteItem` - Функция удаления
- `role` - Роль пользователя

**Функции:**
- Отображение полной информации о товаре
- Поддержка темной темы
- Анимации и переходы

### vite.config.js

**Конфигурация Vite с автоматическим выбором порта**

```javascript
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import { resolve } from "path";
import net from 'net';

// Функция для проверки доступности порта
const findAvailablePort = async (startPort) => {
    for (let port = startPort; port < startPort + 10; port++) {
        const isAvailable = await new Promise((resolve) => {
            const server = net.createServer();
            server.once('error', (err) => {
                resolve(err.code !== 'EADDRINUSE');
            });
            server.once('listening', () => {
                server.close();
                resolve(true);
            });
            server.listen(port);
        });
        
        if (isAvailable) {
            return port;
        }
    }
    return null;
};

export default defineConfig(async () => {
    const port = await findAvailablePort(5173);
    
    return {
        plugins: [react()],
        root: resolve(__dirname, "."),
        build: {
            outDir: "dist",
            emptyOutDir: true,
            rollupOptions: {
                input: {
                    main: resolve(__dirname, "index.html"),
                },
            },
        },
        server: {
            port: port || 5173,
            strictPort: false,
            open: true,
        },
    };
});
```

**Особенности:**
- Автоматический выбор свободного порта
- Поддержка переменных окружения через `import.meta.env`
- Настройка CSP заголовков для безопасности

### Переменные окружения

Проект поддерживает следующие переменные окружения:

**Фронтэнд (.env файл):**
```
VITE_SERVER_URL=http://localhost:3001
```

**Бэкэнд (server/.env файл):**
```
PORT=3001
```

**Как использовать:**
1. Создайте файл `.env` в корне проекта
2. Укажите нужные переменные
3. Перезапустите приложение

### service-worker.js

**Service Worker для PWA**

**Функции:**
- Кэширование ресурсов
- Оффлайн поддержка
- Обновление кэша

### Content Security Policy (CSP)

Проект использует CSP для повышения безопасности. Основные правила:

**Заголовки CSP:**
- `default-src 'self'` - Все ресурсы загружаются только с текущего домена
- `script-src 'self' 'unsafe-inline' 'unsafe-eval'` - Разрешены inline скрипты и eval
- `style-src 'self' 'unsafe-inline'` - Разрешены inline стили
- `connect-src 'self' http://localhost:*` - Разрешены подключения к локальному серверу

**Как изменить:**
1. Отредактируйте файл `server/server.js`
2. Измените CSP заголовки в middleware
3. Перезапустите сервер

### manifest.webmanifest

**Манифест PWA**

```json
{
  "name": "Inventory PWA",
  "short_name": "Inventory",
  "description": "Inventory Management System",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#4f46e5",
  "icons": [
    {
      "src": "favicon.ico",
      "sizes": "48x48",
      "type": "image/x-icon"
    },
    {
      "src": "icon-72x72.png",
      "sizes": "72x72",
      "type": "image/png"
    },
    {
      "src": "icon-96x96.png",
      "sizes": "96x96",
      "type": "image/png"
    },
    {
      "src": "icon-128x128.png",
      "sizes": "128x128",
      "type": "image/png"
    },
    {
      "src": "icon-144x144.png",
      "sizes": "144x144",
      "type": "image/png"
    },
    {
      "src": "icon-152x152.png",
      "sizes": "152x152",
      "type": "image/png"
    },
    {
      "src": "icon-192x192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "icon-384x384.png",
      "sizes": "384x384",
      "type": "image/png"
    },
    {
      "src": "icon-512x512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
```

## 🛠️ Возможности для модификации

1. **Добавить новые категории товаров**
2. **Улучшить систему аутентификации (JWT)**
3. **Добавить экспорт/импорт данных**
4. **Улучшить оффлайн функциональность**
5. **Добавить статистику и отчеты**
6. **Интеграция с облачными сервисами**
7. **Улучшить UI/UX дизайн**

## 🐛 Решение распространенных проблем

### Порт уже занят
**Проблема:** При запуске сервера или Vite вы видите ошибку "Port already in use"

**Решение:**
1. Освободите порт вручную:
   ```bash
   netstat -ano | findstr PORT_NUMBER
   taskkill /PID PID_NUMBER /F
   ```
2. Или используйте автоматический выбор порта (уже настроено в проекте)

### Ошибка с React хуками
**Проблема:** "Invalid hook call. Hooks can only be called inside of the body of a function component"

**Решение:**
1. Убедитесь, что хуки вызываются только в компонентах React
2. Проверьте, что нет дубликатов React в проекте
3. Убедитесь, что все компоненты правильно импортируют React

### Ошибка CSP
**Проблема:** "Content Security Policy directive"

**Решение:**
1. Отредактируйте CSP заголовки в `server/server.js`
2. Добавьте нужные домены в разрешенные источники
3. Перезапустите сервер

### Vite не находит файлы
**Проблема:** В браузере не загружаются JS/CSS файлы

**Решение:**
1. Убедитесь, что Vite запущен
2. Проверьте, что в `index.html` правильно указаны пути к файлам
3. Очистите кэш браузера

### Сервер не подключается к базе данных
**Проблема:** Ошибка подключения к SQLite

**Решение:**
1. Убедитесь, что файл `DataBase.db3` существует
2. Проверьте права доступа к файлу
3. Перезапустите сервер

## 📋 Лицензия

Проект распостраняется под лицензией MIT.

## 🤝 Контакты

Для вопросов и поддержки:
- Email: support@inventory-pwa.com
- GitHub: github.com/inventory-pwa

---

**Последнее обновление**: 05.01.2026
**Версия**: 1.0.0